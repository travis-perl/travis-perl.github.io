helper_dir="$(mktemp -d -t travis-perl-helpers-XXXXXX)";
git clone "${TRAVIS_PERL_REPO:-git://github.com/travis-perl/helpers}" "$helper_dir";
(test -n "$TRAVIS_PERL_BRANCH" && cd "$helper_dir" && git checkout "$TRAVIS_PERL_BRANCH");
source "$helper_dir/init"
